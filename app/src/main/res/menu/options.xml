<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/Menu"
        android:title="Menu"
        app:showAsAction="always">
        <!-- "file" submenu -->
        <menu>
            <item android:id="@+id/ResetButton"
                android:title="Reset"
                android:layout_height="match_parent" />
            <item android:id="@+id/decreaseHome"
                android:title="Reduce Home Score"
                android:layout_height="match_parent"/>
            <item android:id="@+id/decreaseAway"
                android:title="Reduce Away Score"
                android:layout_height="match_parent"/>
        </menu>
    </item>
</menu>